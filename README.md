# 🎓 Tutor Platform: Scalable LMS & CRM for Tutors

**Tutor Platform** — это высокопроизводительная SaaS-платформа, предназначенная для автоматизации образовательного процесса. Проект объединяет функционал LMS (Learning Management System) и CRM, обеспечивая полную изоляцию данных между преподавателями и гибкое управление учебными траекториями.

> **Статус проекта:** 🏗️ В активной разработке (WIP). Реализовано архитектурное ядро, система безопасности и модуль управления пользователями.
> **Разработчик:** Solo Project.

---

## 🛠 Технологический стек

| Слой | Технологии |
| --- | --- |
| **Backend** | **FastAPI** (Asynchronous), Python 3.12+ |
| **Database** | **PostgreSQL**, **SQLModel** (Modern SQLAlchemy 2.0 wrapper) |
| **Driver** | **asyncpg** (высокопроизводительный асинхронный драйвер) |
| **Storage** | **Yandex Cloud (S3)** для хранения медиаконтента |
| **Security** | **JWT** (HttpOnly Cookies), **RBAC**, Custom Middleware |
| **Migrations** | **Alembic** |
| **CLI Tools** | **Typer** (для административных скриптов) |

---

## 🚀 Ключевые архитектурные решения

### 1. Безопасность и контроль доступа (Security First)

Реализована кастомная система аутентификации, минимизирующая риски атак:

* **JWT в HttpOnly Cookies:** Токены защищены от кражи через XSS-скрипты.
* **Custom JWT Middleware:** Автоматическая проверка авторизации для каждого запроса, обработка истекших сессий и редиректы на уровне протокола.
* **Role-Based Access Control (RBAC):** Декораторы для проверки ролей (Superuser, Teacher, Student) на эндпоинтах.
* **Validation:** Строгая валидация сложности паролей и уникальности данных на базе Pydantic v2.

### 2. Multi-tenancy и Изоляция данных

Архитектура спроектирована по принципу полной изоляции контента:

* **Data Leak Prevention:** Учитель имеет доступ только к "своим" ученикам и материалам. Ученик видит исключительно контент своего преподавателя.
* **Database Partitioning:** Для обеспечения высокой производительности при миллионах записей применено **хеш-партицирование** таблицы `LessonAssignment` по `teacher_id`.

### 3. Гибкая LMS-система

* **Интерактивные уроки:** Хранение структуры уроков и квизов в формате **JSONB**, что позволяет комбинировать текст, видео, PDF и интерактивные задания в одной сущности без оверхеда на уровне таблиц.
* **S3 Интеграция:** Бесшовная загрузка и отдача тяжелых файлов через облачное объектное хранилище.

### 4. Геймификация и Календарь

* **Engagement:** Двухуровневая система вознаграждений (баллы за уроки + монеты для покупки кастомизации профиля).
* **Smart Scheduling:** Календарь с автоматической генерацией ссылок на конференции (VK Calls) и логикой автоудаления устаревших записей для поддержания чистоты БД.

---

## 📂 Структура проекта

```text
.
├── app
│   ├── api          # Роуты (auth, admin, lms, calendar)
│   ├── core         # Конфигурация, БД, Security Middleware
│   ├── models       # Описание сущностей (User, Teacher, Student, Lesson)
│   ├── schemas      # Pydantic модели (валидация входных/выходных данных)
│   ├── services     # Тяжелая бизнес-логика
│   ├── templates    # Frontend (Jinja2 / SSR элементы)
│   └── manage.py    # CLI утилиты для управления платформой
├── migrations       # Миграции базы данных (Alembic)
├── .env             # Переменные окружения
└── alembic.ini      # Конфигурация Alembic

```

---

## ⚙️ Реализованный функционал (Backend)

* **Auth System:**
* Асинхронный хэшинг паролей (`pwdlib`).
* Middleware для извлечения и валидации JWT из кук.
* Автоматический редирект неавторизованных пользователей на страницу Login.


* **User Management:**
* Иерархическая модель создания пользователей: Admin -> Teacher -> Student.
* Валидация уникальности Email через Depends-инъекции.


* **Database:**
* Автоматическое управление полями `created_at` и `updated_at` через миксины.
* Сложные связи Many-to-Many с хранением метаданных прогресса.



---

## 🛠 Установка

1. **Клонируйте репозиторий:**

```bash
git clone https://github.com/your-profile/tutorflow.git

```

2. **Настройте переменные окружения:**
Создайте `.env` и укажите данные для PostgreSQL, (S3) и JWT_SECRET.
```
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=
DB_NAME=

# jwt
SECRET_KEY = ""
ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_MINUTES = 60

# without authentication
EXCLUDED_PATHS = ["/auth/login", "/docs", "/openapi.json", "/redoc"]
```

4. **Запустите миграции:**

```bash
alembic upgrade head

```

4. **Создайте суперпользователя:**

```bash
python -m app.manage create-admin --email admin@test.com --password YourStrongPassword123! --name Admin --surname User --second_name Admin --birthday 1990-01-01

```

5. **Запуск приложения:**

```bash
uvicorn app.main:app --reload

```

---

*Проект находится в стадии активной разработки. После запуска доступны ендпойнты (c фронтендом): /auth/login, superuser/create-teacher*

---

<img width="1920" height="1002" alt="Снимок экрана от 2026-02-25 23-55-12" src="https://github.com/user-attachments/assets/24798a0b-8069-4856-8624-b789f9146ccd" />

<img width="1920" height="1002" alt="Снимок экрана от 2026-02-25 23-57-38" src="https://github.com/user-attachments/assets/0f019517-5c11-41f7-85c4-dedc966dd4fc" />
